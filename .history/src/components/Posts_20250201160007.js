import { Card } from "@mui/material";

export default function Posts(img, ){
    const handleRedirect = (url) => {
        
    };
    return(
        <Card>
            <Img fluid={}/>
             <Card.Body>
                <div>

                </div>
             </Card.Body>
        </Card>
    )
}