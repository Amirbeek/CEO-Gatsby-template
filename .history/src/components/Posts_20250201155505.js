import { Card } from "@mui/material";

export default function Posts(){
    const handleRedirect = (url) => {
        
    };
    return(
        <Card>
            <Card.img>
                
            </Card.img>
        </Card>
    )
}