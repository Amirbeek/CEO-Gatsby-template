import { Card } from "@mui/material";

export default function Posts(img, url, title, type){
    const handleRedirect = (imgUrl) => {
        
    };
    return(
        <Card>
            <Img fluid={im}/>
             <Card.Body>
                <div>

                </div>
             </Card.Body>
        </Card>
    )
}